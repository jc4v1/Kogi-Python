# Use the Kogi-App within a docker container.

Given docker and docker compose is installed, you can execute

``docker compose up -d``

then this will create a Docker image and start a container which offers the Kogi-App on port 8866 on localhost.